agent:
  name: triage-agent
  description: Front-desk routing agent.

  instructions:
    file: ../../personas/triage-agent.md

  model: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}

  tools:
    - type: code_interpreter
    # The triage agent intentionally has no file_search / azure_ai_search;
    # KB queries should be handed off to KBAgent.
