agent:
  name: billing-agent
  description: Billing / refund / invoice specialist.

  instructions:
    file: ../../personas/billing-agent.md

  model: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}

  tools:
    - type: code_interpreter
    # Uncomment when you have a vector store with billing policy docs:
    # - type: file_search
    #   file_search:
    #     vector_store_ids:
    #       - ${BILLING_DOCS_VECTOR_STORE_ID}
    # - type: memory_search
    #   memory_search:
    #     store_id: ${SUPPORT_MEMORY_STORE_ID}
    #     scope: "{{$userId}}"
