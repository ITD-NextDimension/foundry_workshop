# Workshop Observability

本目录只剩一个本地路径：

- [`local/`](./local) — 单文件 HTML + `fetch-traces.ps1`，凭学员 SP 直接拉自己 hosted agent 的运行数据。

> 旧版本里的 `swa/`（Static Web App + Functions + Application Insights）已经移除。
> 新流程**不登 portal、不依赖 App Insights**，全部走 Foundry agents 数据平面 API。
